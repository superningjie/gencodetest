# 列表插件

原文链接：https://vip.kingdee.com/knowledge/specialDetail/218022218066869248?category=720699175037349632&id=720676775960893696&type=Knowledge&productLineId=29&lang=zh-CN

## 列表插件

 __

[![金蝶云社区-aohailin](https://vip.kingdee.com/download/010162cfa9d6437711e8b441060400ef5315.png)](javascript:;)

aohailin

更新于 2025-06-13 15:08

浏览数： 2,146 

### 1、插件示例

### 定义一个列表插件
    
    
    import { AbstractListPlugin } from "@cosmic/bos-core/kd/bos/list/plugin";
    import { SetFilterEvent, FilterContainerSearchClickArgs } from "@cosmic/bos-core/kd/bos/form/events";
    import { ItemClickEvent } from "@cosmic/bos-core/kd/bos/form/control/events";
    
    class MyPlugin extends AbstractListPlugin {
        setFilter(e: SetFilterEvent): void {
            super.setFilter(e);
        }
        filterContainerSearchClick(e: FilterContainerSearchClickArgs): void {
            super.filterContainerSearchClick(e);
        }
        itemClick(e: ItemClickEvent): void {
            super.itemClick(e);
        }
    }
    
    let plugin = new MyPlugin();
    export { plugin };

### 2、事件方法

### 列表打包数据前，批量调整字段值
    
    
    beforePackageData(e: BeforePackageDataEvent): void {
        for (let row of e.getPageData()) {
            row.set("combofield", "cmb");
        }
    }

### 列表显示单据前，传入自定义参数
    
    
    beforeShowBill(e: BeforeShowBillFormEvent): void {
        let showParameter = e.getParameter() as BillShowParameter;
        showParameter.setCustomParam("myparam", "paramvalue");
    }

### 列表过滤面板中，给F7列表添加过滤条件
    
    
    filterContainerBeforeF7Select(args: BeforeFilterF7SelectEvent): void {
        let fieldName = args.getFieldName()
        if ("org.id" == fieldName) {
            args.getQfilters().add(new QFilter("number", "=", "00"));
        }
    }

### 列表过滤面板中，添加方案过滤选项
    
    
    filterContainerInit(args: FilterContainerInitArgs): void {
        let filterColumn = new SchemeFilterColumn("combofield");
        args.addFilterColumn(filterColumn);
    }

### 给列表增加自定义过滤条件
    
    
    setFilter(e: SetFilterEvent): void {
        e.addCustomQFilter(new QFilter("combofield", "=", "1"));
    }

### 给列表设置排序字段
    
    
    setFilter(e: SetFilterEvent): void {
        e.setOrderBy("bizdate desc, billno desc");
    }

### 给列表动态增加列
    
    
    beforeCreateListColumns(event: BeforeCreateListColumnsArgs): void {
        let col = new ListColumn();
        col.setCaption(new LocaleString("Creator"));
        col.setKey("create.name");
        col.setListFieldKey("create.name");
        col.setFieldName("create.name");
        event.addListColumn(col);
    }

### 在列表中点击超链接打开界面
    
    
    billListHyperLinkClick(args: HyperLinkClickArgs): void {
        let fieldKey = args.getFieldName();
        // 监听sourcebillno字段
        if (fieldKey == "sourcebillno") {
            // 取消默认行为
            args.setCancel(true);
            let billLinkEvent = args.getHyperLinkClickEvent() as BillListHyperLinkClickEvent;
            let billList = billLinkEvent.getSource() as BillList;
            let entityKey = billList.getEntityId();
            let pk = billList.getFocusRowPkId();
            // 根据主键加载当前行数据
            let billObj = BusinessDataServiceHelper.loadSingle(pk, entityKey, "sourcebilltype, sourcebillid");
            if (billObj != null) {
                // 构建弹窗showParameter对象
                let showParameter = new BillShowParameter();
                showParameter.setFormId(billObj.getString("sourcebilltype"));
                showParameter.setPkId(billObj.getLong("sourcebillid"));
                showParameter.setStatus(OperationStatus.VIEW);
                showParameter.getOpenStyle().setShowType(ShowType.MainNewTabPage);
                // 显示弹窗
                plugin.getView().showForm(showParameter);
            }
        }
    }

### 列表常用过滤面板，增加取数条件
    
    
    filterColumnSetFilter(args: SetFilterEvent): void {
        if ("org.name" == args.getFieldName()) {
            args.addCustomQFilter(new QFilter("number", "=", "00"));
        }
    }

### 列表过滤面板中，添加常用过滤选项
    
    
    filterContainerInit(args: FilterContainerInitArgs): void {
        let filterColumn = new CommonFilterColumn("billstatus");
        args.addFilterColumn(filterColumn);
    }

### 列表过滤面板中，点击查询追加快捷条件
    
    
    filterContainerSearchClick(args: FilterContainerSearchClickArgs): void {
        args.addFastFilter("billno", "A");
    }

### 列表打包字段到前端，调整单元格显示值
    
    
    packageData(e: PackageDataEvent): void {
        if ("textfield" == e.getColKey() && "text" == e.getFormatValue()) {
            e.setFormatValue("Formatted Content");
        }
    }

  


__上一篇：操作插件

下一篇：转换插件

 __

5.0 2人评分

内容反馈

*  __评论
收藏 1 

![复制链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_link_pc.png)复制链接

![复制标题链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_l.png)复制标题+链接

分享

 __

扫一扫用手机继续查看

![vip.kingdee.com]()

 __手机播放

编辑举报
