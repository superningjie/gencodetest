# 容器控件——轮播容器

原文链接：https://vip.kingdee.com/knowledge/specialDetail/218022218066869248?category=218035632357421312&id=226624290336908800&type=Knowledge&productLineId=29&lang=zh-CN

## 容器控件——轮播容器

 __

[![金蝶云社区-逮虾的虎](https://vip.kingdee.com/download/01011d85de89730d4f439daeb640456f5717.png)](javascript:;)

逮虾的虎

更新于 2025-01-22 14:54

浏览数： 3,850 

一级标题

二级标题

三级标题

四级标题

五级标题

六级标题

添加图片链接

上传图片 
    
    
      
    # 1 功能介绍
    轮播容器控件可以在一定空间内循环展示多个信息；展示的内容不仅可以是图片，还可以是按钮、标签和矢量图标。
    # 2 控件对象
    `kd.bos.ext.form.control.MessageCarouselContainer`
    # 3 视觉展示
    ![1.gif](/download/0100e00b723b063c42a1a21d44c39371247d.gif)
    # 4 属性说明
    ## 4.1 通用属性
    >通用属性包含字段和控件的一些公有的属性，如宽高，帮助文本等等。请参考[通用属性](https://vip.kingdee.com/article/215559076720798976)
    ## 4.2 样式属性
    >样式属性是每个控件在设计器右侧样式栏可以设置的属性，请参考[样式属性](https://vip.kingdee.com/article/252017936767406336)
    ## 4.3 业务属性
    
    | 属性名 | 类型 | 默认值 | 说明 | PC | Mobile |
    | --- | --- | --- | --- | --- | --- |
    | 轮播数量 | 整数 | 1 | 在面板显示的子项数目 | V4.0及以上 | V4.0及以上 |
    | 轮播方向 | 下拉 | 由下往上 | 轮播内容切换的方向，有四个值：由下往上、由上往下、由左往右、由右往左 | V4.0及以上 | V4.0及以上 |
    | 速度 | 整数 | 1000 | 循环播放时项目之间的延迟时间，单位ms | V4.0及以上 | V4.0及以上 |
    | 延迟 | 整数 | 1000 | 多久后轮播下一个子项，也就是轮播项目停留的时间，单位ms | V4.0及以上 | V4.0及以上 |
    | 显示指示点 | 复选框 | false | 设置是否显示轮播的指示点 | V4.0及以上 | V4.0及以上 |
    | 指示点位置 | 下拉 | 下 | 设置指示点位置 | V4.0及以上 | V4.0及以上 |
    
    # 5 轮播容器示例介绍
    ## 5.1 控件方法
    
    | 方法名 | 参数 | 说明 |  |
    | --- | --- | --- | --- |
    | setData | List<Map<String, Object>> | 添加轮播数据 |  |
    
    ```java
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("控件标识");
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    String[] urls = new String[] {"url1", "url2", "url3"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"t1", "t2", "t3"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("labelap", propsMap);
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);
      controlsMap.put("imageap", propsMap);
      
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    
    ```
    轮播容器内支持`按钮`，`标签`，`图片`，`字体图标几种控件`，设置数据时，对应的数据标识如下：
    
    
    | 控件类型 | 属性名 | 说明 |
    | --- | --- | --- |
    | 按钮 | text | 设置按钮文本 |
    | 标签 | text | 设置标签文本 |
    | 图片 | imageKey | 设置图片路径 |
    | 字体图标 | fontClass | 设置图标名称 |
    
    
    ## 5.2 示例
    
    1. 首先需要在设计器内设计一个轮播容器，例如我们创建一个拥有图片和文本的字段的组件，容器的字段标识为`kdtest_messagecarouselco2`，内部的图片和文本的标识分别为`kdtest_imageap2`及`kdtest_labelap12`,字体图标标识为`kdtest_vectorap2`
    ![2.png](/download/0100653beb7666f94756bdc286475023ef90.png)
    2.通过setData添加数据
    ```java
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("kdtest_messagecarouselco2"); //示例中我们的标识是`lbtap`
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    // 假设我们在该路径下有对应的数据
    String[] urls = new String[] {"/private/subscribe_pic1.png", "/private/subscribe_pic2.png", "/private/subscribe_pic3.png"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"客户至上，用心服务，金蝶云总经理李帆给您的亲笔信", "致敬每一位勇敢拥抱数字化浪潮的人", "企业数
    字化转型高峰论坛"};
    *// 如果含有字体图标*
    String[] iconfonts = new String[] {"kdfont kdfont-tianjia", "kdfont kdfont-wendang", "kdfont kdfont-shangchuan"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {foopr
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("kdtest_labelap12", propsMap); // kdtest_labelap12是示例中的文本控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);  
      controlsMap.put("kdtest_imageap2", propsMap); // kdtest_imageap2是图片控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("fontClass", iconfonts[i]);
      controlsMap.put("kdtest_vectorap2", propsMap); // kdtest_vectorap2是字体图标标识
     
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    ```
      3. 返回数据示例
    
    *   在对应的请求中可以找到我们刚刚设置的数据
    ![3.png](/download/01008c371e899be24ddb92724b3b449399a0.png)
    
    4. 页面展示如图
    ![4.png](/download/01001e0ba84ae2064b9fbec42423cce07844.png)
    
    5. 如需进行事件监听，可以采用以下监听方法
    
    * 事件监听
    
    ```java
    *// 实现「ListboxClickListener」接口*public class TestPlugin extends AbstractFormPlugin implements ItemClickListener, ClickListner {
      public void registerListener(EventObject event) {
    
        *// 注册监听*
        MessageCarouselContainer mcc = this.getView().getControl("控件标识");
        mcc.addItemClickListener(this);
        mcc.addClickListener(this);
      }
    
      *// 一行中的子项点击事件 发生之前，可以取消事件继续*
      public void beforeItemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 一行中的子项点击事件*
      public void itemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件 发生之前，可以取消事件继续*
      public void beforeClick(BeforeClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件*
      public void click(BeforeClickEvnet event) {
        *// do something...*
      }
    }
    
     

# 1 功能介绍

轮播容器控件可以在一定空间内循环展示多个信息；展示的内容不仅可以是图片，还可以是按钮、标签和矢量图标。

# 2 控件对象

`kd.bos.ext.form.control.MessageCarouselContainer`

# 3 视觉展示

![1.gif](https://vip.kingdee.com/download/0100e00b723b063c42a1a21d44c39371247d.gif)

# 4 属性说明

## 4.1 通用属性

> 通用属性包含字段和控件的一些公有的属性，如宽高，帮助文本等等。请参考[通用属性](https://vip.kingdee.com/article/215559076720798976)

## 4.2 样式属性

> 样式属性是每个控件在设计器右侧样式栏可以设置的属性，请参考[样式属性](https://vip.kingdee.com/article/252017936767406336)

## 4.3 业务属性

属性名 | 类型 | 默认值 | 说明 | PC | Mobile  
---|---|---|---|---|---  
轮播数量 | 整数 | 1 | 在面板显示的子项数目 | V4.0及以上 | V4.0及以上  
轮播方向 | 下拉 | 由下往上 | 轮播内容切换的方向，有四个值：由下往上、由上往下、由左往右、由右往左 | V4.0及以上 | V4.0及以上  
速度 | 整数 | 1000 | 循环播放时项目之间的延迟时间，单位ms | V4.0及以上 | V4.0及以上  
延迟 | 整数 | 1000 | 多久后轮播下一个子项，也就是轮播项目停留的时间，单位ms | V4.0及以上 | V4.0及以上  
显示指示点 | 复选框 | false | 设置是否显示轮播的指示点 | V4.0及以上 | V4.0及以上  
指示点位置 | 下拉 | 下 | 设置指示点位置 | V4.0及以上 | V4.0及以上  
  
# 5 轮播容器示例介绍

## 5.1 控件方法

方法名 | 参数 | 说明 |   
---|---|---|---  
setData | List<Map<String, Object>> | 添加轮播数据 |   
      
    
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("控件标识");
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    String[] urls = new String[] {"url1", "url2", "url3"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"t1", "t2", "t3"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("labelap", propsMap);
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);
      controlsMap.put("imageap", propsMap);
      
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    
    

轮播容器内支持`按钮`，`标签`，`图片`，`字体图标几种控件`，设置数据时，对应的数据标识如下：

控件类型 | 属性名 | 说明  
---|---|---  
按钮 | text | 设置按钮文本  
标签 | text | 设置标签文本  
图片 | imageKey | 设置图片路径  
字体图标 | fontClass | 设置图标名称  
  
## 5.2 示例

  1. 首先需要在设计器内设计一个轮播容器，例如我们创建一个拥有图片和文本的字段的组件，容器的字段标识为`kdtest_messagecarouselco2`，内部的图片和文本的标识分别为`kdtest_imageap2`及`kdtest_labelap12`,字体图标标识为`kdtest_vectorap2`  
![2.png](https://vip.kingdee.com/download/0100653beb7666f94756bdc286475023ef90.png)  
2.通过setData添加数据


    
    
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("kdtest_messagecarouselco2"); //示例中我们的标识是`lbtap`
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    // 假设我们在该路径下有对应的数据
    String[] urls = new String[] {"/private/subscribe_pic1.png", "/private/subscribe_pic2.png", "/private/subscribe_pic3.png"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"客户至上，用心服务，金蝶云总经理李帆给您的亲笔信", "致敬每一位勇敢拥抱数字化浪潮的人", "企业数
    字化转型高峰论坛"};
    *// 如果含有字体图标*
    String[] iconfonts = new String[] {"kdfont kdfont-tianjia", "kdfont kdfont-wendang", "kdfont kdfont-shangchuan"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {foopr
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("kdtest_labelap12", propsMap); // kdtest_labelap12是示例中的文本控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);  
      controlsMap.put("kdtest_imageap2", propsMap); // kdtest_imageap2是图片控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("fontClass", iconfonts[i]);
      controlsMap.put("kdtest_vectorap2", propsMap); // kdtest_vectorap2是字体图标标识
     
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    

  3. 返回数据示例


  * 在对应的请求中可以找到我们刚刚设置的数据  
![3.png](https://vip.kingdee.com/download/01008c371e899be24ddb92724b3b449399a0.png)


  4. 页面展示如图  
![4.png](https://vip.kingdee.com/download/01001e0ba84ae2064b9fbec42423cce07844.png)

  5. 如需进行事件监听，可以采用以下监听方法



  * 事件监听


    
    
    *// 实现「ListboxClickListener」接口*public class TestPlugin extends AbstractFormPlugin implements ItemClickListener, ClickListner {
      public void registerListener(EventObject event) {
    
        *// 注册监听*
        MessageCarouselContainer mcc = this.getView().getControl("控件标识");
        mcc.addItemClickListener(this);
        mcc.addClickListener(this);
      }
    
      *// 一行中的子项点击事件 发生之前，可以取消事件继续*
      public void beforeItemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 一行中的子项点击事件*
      public void itemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件 发生之前，可以取消事件继续*
      public void beforeClick(BeforeClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件*
      public void click(BeforeClickEvnet event) {
        *// do something...*
      }
    }
    
    

<h1><a id="1__0"></a>1 功能介绍</h1> <p>轮播容器控件可以在一定空间内循环展示多个信息；展示的内容不仅可以是图片，还可以是按钮、标签和矢量图标。</p> <h1><a id="2__2"></a>2 控件对象</h1> <p><code>kd.bos.ext.form.control.MessageCarouselContainer</code></p> <h1><a id="3__4"></a>3 视觉展示</h1> <p><img src="/download/0100e00b723b063c42a1a21d44c39371247d.gif" alt="1.gif" /></p> <h1><a id="4__6"></a>4 属性说明</h1> <h2><a id="41__7"></a>4.1 通用属性</h2> <blockquote> <p>通用属性包含字段和控件的一些公有的属性，如宽高，帮助文本等等。请参考<a href="https://vip.kingdee.com/article/215559076720798976" target="_blank">通用属性</a></p> </blockquote> <h2><a id="42__9"></a>4.2 样式属性</h2> <blockquote> <p>样式属性是每个控件在设计器右侧样式栏可以设置的属性，请参考<a href="https://vip.kingdee.com/article/252017936767406336" target="_blank">样式属性</a></p> </blockquote> <h2><a id="43__11"></a>4.3 业务属性</h2> <table> <thead> <tr> <th>属性名</th> <th>类型</th> <th>默认值</th> <th>说明</th> <th>PC</th> <th>Mobile</th> </tr> </thead> <tbody> <tr> <td>轮播数量</td> <td>整数</td> <td>1</td> <td>在面板显示的子项数目</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> <tr> <td>轮播方向</td> <td>下拉</td> <td>由下往上</td> <td>轮播内容切换的方向，有四个值：由下往上、由上往下、由左往右、由右往左</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> <tr> <td>速度</td> <td>整数</td> <td>1000</td> <td>循环播放时项目之间的延迟时间，单位ms</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> <tr> <td>延迟</td> <td>整数</td> <td>1000</td> <td>多久后轮播下一个子项，也就是轮播项目停留的时间，单位ms</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> <tr> <td>显示指示点</td> <td>复选框</td> <td>false</td> <td>设置是否显示轮播的指示点</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> <tr> <td>指示点位置</td> <td>下拉</td> <td>下</td> <td>设置指示点位置</td> <td>V4.0及以上</td> <td>V4.0及以上</td> </tr> </tbody> </table> <h1><a id="5__22"></a>5 轮播容器示例介绍</h1> <h2><a id="51__23"></a>5.1 控件方法</h2> <table> <thead> <tr> <th>方法名</th> <th>参数</th> <th>说明</th> <th></th> </tr> </thead> <tbody> <tr> <td>setData</td> <td>List&lt;Map&lt;String, Object&gt;&gt;</td> <td>添加轮播数据</td> <td></td> </tr> </tbody> </table> <pre><div class="hljs"><code class="lang-java">*<span class="hljs-comment">/*** * * 向轮播容器中动态添加三条数据* * * 包含 图片、标签控件。* * */</span>* MessageCarouselContainer mcc = <span class="hljs-keyword">this</span>.getControl(<span class="hljs-string">"控件标识"</span>); List&lt;Map&lt;String, Object&gt;&gt; data = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(); *<span class="hljs-comment">// 图片控件url数组*</span> String[] urls = <span class="hljs-keyword">new</span> String[] {<span class="hljs-string">"url1"</span>, <span class="hljs-string">"url2"</span>, <span class="hljs-string">"url3"</span>}; *<span class="hljs-comment">// 标签控件显示内容数组*</span> String[] texts = <span class="hljs-keyword">new</span> String[] {<span class="hljs-string">"t1"</span>, <span class="hljs-string">"t2"</span>, <span class="hljs-string">"t3"</span>}; *<span class="hljs-comment">// 放内容map*</span> Map&lt;String, Object&gt; propsMap = <span class="hljs-keyword">null</span>; *<span class="hljs-comment">// 控件map，key为控件类型*</span> Map&lt;String, Object&gt; controlsMap = <span class="hljs-keyword">null</span>; <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; urls.length; i++;) { propsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); controlsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); propsMap.put(<span class="hljs-string">"text"</span>, texts[i]); controlsMap.put(<span class="hljs-string">"labelap"</span>, propsMap); propsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); propsMap.put(<span class="hljs-string">"imageKey"</span>, urls[i]); controlsMap.put(<span class="hljs-string">"imageap"</span>, propsMap); data.add(controlsMap); } mcc.setData(data); </code></div></pre> <p>轮播容器内支持<code>按钮</code>，<code>标签</code>，<code>图片</code>，<code>字体图标几种控件</code>，设置数据时，对应的数据标识如下：</p> <table> <thead> <tr> <th>控件类型</th> <th>属性名</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td>按钮</td> <td>text</td> <td>设置按钮文本</td> </tr> <tr> <td>标签</td> <td>text</td> <td>设置标签文本</td> </tr> <tr> <td>图片</td> <td>imageKey</td> <td>设置图片路径</td> </tr> <tr> <td>字体图标</td> <td>fontClass</td> <td>设置图标名称</td> </tr> </tbody> </table> <h2><a id="52__73"></a>5.2 示例</h2> <ol> <li>首先需要在设计器内设计一个轮播容器，例如我们创建一个拥有图片和文本的字段的组件，容器的字段标识为<code>kdtest_messagecarouselco2</code>，内部的图片和文本的标识分别为<code>kdtest_imageap2</code>及<code>kdtest_labelap12</code>,字体图标标识为<code>kdtest_vectorap2</code><br /> <img src="/download/0100653beb7666f94756bdc286475023ef90.png" alt="2.png" /><br /> 2.通过setData添加数据</li> </ol> <pre><div class="hljs"><code class="lang-java">*<span class="hljs-comment">/*** * * 向轮播容器中动态添加三条数据* * * 包含 图片、标签控件。* * */</span>* MessageCarouselContainer mcc = <span class="hljs-keyword">this</span>.getControl(<span class="hljs-string">"kdtest_messagecarouselco2"</span>); <span class="hljs-comment">//示例中我们的标识是`lbtap`</span> List&lt;Map&lt;String, Object&gt;&gt; data = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(); *<span class="hljs-comment">// 图片控件url数组*</span> <span class="hljs-comment">// 假设我们在该路径下有对应的数据</span> String[] urls = <span class="hljs-keyword">new</span> String[] {<span class="hljs-string">"/private/subscribe_pic1.png"</span>, <span class="hljs-string">"/private/subscribe_pic2.png"</span>, <span class="hljs-string">"/private/subscribe_pic3.png"</span>}; *<span class="hljs-comment">// 标签控件显示内容数组*</span> String[] texts = <span class="hljs-keyword">new</span> String[] {<span class="hljs-string">"客户至上，用心服务，金蝶云总经理李帆给您的亲笔信"</span>, <span class="hljs-string">"致敬每一位勇敢拥抱数字化浪潮的人"</span>, <span class="hljs-string">"企业数 字化转型高峰论坛"</span>}; *<span class="hljs-comment">// 如果含有字体图标*</span> String[] iconfonts = <span class="hljs-keyword">new</span> String[] {<span class="hljs-string">"kdfont kdfont-tianjia"</span>, <span class="hljs-string">"kdfont kdfont-wendang"</span>, <span class="hljs-string">"kdfont kdfont-shangchuan"</span>}; *<span class="hljs-comment">// 放内容map*</span> Map&lt;String, Object&gt; propsMap = <span class="hljs-keyword">null</span>; *<span class="hljs-comment">// 控件map，key为控件类型*</span> Map&lt;String, Object&gt; controlsMap = <span class="hljs-keyword">null</span>; <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; urls.length; i++;) {foopr propsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); controlsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); propsMap.put(<span class="hljs-string">"text"</span>, texts[i]); controlsMap.put(<span class="hljs-string">"kdtest_labelap12"</span>, propsMap); <span class="hljs-comment">// kdtest_labelap12是示例中的文本控件标识</span> propsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); propsMap.put(<span class="hljs-string">"imageKey"</span>, urls[i]); controlsMap.put(<span class="hljs-string">"kdtest_imageap2"</span>, propsMap); <span class="hljs-comment">// kdtest_imageap2是图片控件标识</span> propsMap = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(); propsMap.put(<span class="hljs-string">"fontClass"</span>, iconfonts[i]); controlsMap.put(<span class="hljs-string">"kdtest_vectorap2"</span>, propsMap); <span class="hljs-comment">// kdtest_vectorap2是字体图标标识</span> data.add(controlsMap); } mcc.setData(data); </code></div></pre> <ol start="3"> <li>返回数据示例</li> </ol> <ul> <li>在对应的请求中可以找到我们刚刚设置的数据<br /> <img src="/download/01008c371e899be24ddb92724b3b449399a0.png" alt="3.png" /></li> </ul> <ol start="4"> <li> <p>页面展示如图<br /> <img src="/download/01001e0ba84ae2064b9fbec42423cce07844.png" alt="4.png" /></p> </li> <li> <p>如需进行事件监听，可以采用以下监听方法</p> </li> </ol> <ul> <li>事件监听</li> </ul> <pre><div class="hljs"><code class="lang-java">*<span class="hljs-comment">// 实现「ListboxClickListener」接口*public class TestPlugin extends AbstractFormPlugin implements ItemClickListener, ClickListner {</span> <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerListener</span><span class="hljs-params">(EventObject event)</span> </span>{ *<span class="hljs-comment">// 注册监听*</span> MessageCarouselContainer mcc = <span class="hljs-keyword">this</span>.getView().getControl(<span class="hljs-string">"控件标识"</span>); mcc.addItemClickListener(<span class="hljs-keyword">this</span>); mcc.addClickListener(<span class="hljs-keyword">this</span>); } *<span class="hljs-comment">// 一行中的子项点击事件 发生之前，可以取消事件继续*</span> <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">beforeItemClick</span><span class="hljs-params">(BeforeItemClickEvnet event)</span> </span>{ *<span class="hljs-comment">// do something...*</span> } *<span class="hljs-comment">// 一行中的子项点击事件*</span> <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">itemClick</span><span class="hljs-params">(BeforeItemClickEvnet event)</span> </span>{ *<span class="hljs-comment">// do something...*</span> } *<span class="hljs-comment">// 行点击事件 发生之前，可以取消事件继续*</span> <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">beforeClick</span><span class="hljs-params">(BeforeClickEvnet event)</span> </span>{ *<span class="hljs-comment">// do something...*</span> } *<span class="hljs-comment">// 行点击事件*</span> <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">click</span><span class="hljs-params">(BeforeClickEvnet event)</span> </span>{ *<span class="hljs-comment">// do something...*</span> } } </code></div></pre>

导航目录 __

# 1 功能介绍

轮播容器控件可以在一定空间内循环展示多个信息；展示的内容不仅可以是图片，还可以是按钮、标签和矢量图标。

# 2 控件对象

`kd.bos.ext.form.control.MessageCarouselContainer`

# 3 视觉展示

![1.gif](https://vip.kingdee.com/download/0100e00b723b063c42a1a21d44c39371247d.gif)

# 4 属性说明

## 4.1 通用属性

> 通用属性包含字段和控件的一些公有的属性，如宽高，帮助文本等等。请参考[通用属性](https://vip.kingdee.com/article/215559076720798976)

## 4.2 样式属性

> 样式属性是每个控件在设计器右侧样式栏可以设置的属性，请参考[样式属性](https://vip.kingdee.com/article/252017936767406336)

## 4.3 业务属性

属性名 | 类型 | 默认值 | 说明 | PC | Mobile  
---|---|---|---|---|---  
轮播数量 | 整数 | 1 | 在面板显示的子项数目 | V4.0及以上 | V4.0及以上  
轮播方向 | 下拉 | 由下往上 | 轮播内容切换的方向，有四个值：由下往上、由上往下、由左往右、由右往左 | V4.0及以上 | V4.0及以上  
速度 | 整数 | 1000 | 循环播放时项目之间的延迟时间，单位ms | V4.0及以上 | V4.0及以上  
延迟 | 整数 | 1000 | 多久后轮播下一个子项，也就是轮播项目停留的时间，单位ms | V4.0及以上 | V4.0及以上  
显示指示点 | 复选框 | false | 设置是否显示轮播的指示点 | V4.0及以上 | V4.0及以上  
指示点位置 | 下拉 | 下 | 设置指示点位置 | V4.0及以上 | V4.0及以上  
  
# 5 轮播容器示例介绍

## 5.1 控件方法

方法名 | 参数 | 说明 |   
---|---|---|---  
setData | List<Map<String, Object>> | 添加轮播数据 |   
      
    
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("控件标识");
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    String[] urls = new String[] {"url1", "url2", "url3"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"t1", "t2", "t3"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("labelap", propsMap);
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);
      controlsMap.put("imageap", propsMap);
      
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    
    

轮播容器内支持`按钮`，`标签`，`图片`，`字体图标几种控件`，设置数据时，对应的数据标识如下：

控件类型 | 属性名 | 说明  
---|---|---  
按钮 | text | 设置按钮文本  
标签 | text | 设置标签文本  
图片 | imageKey | 设置图片路径  
字体图标 | fontClass | 设置图标名称  
  
## 5.2 示例

  1. 首先需要在设计器内设计一个轮播容器，例如我们创建一个拥有图片和文本的字段的组件，容器的字段标识为`kdtest_messagecarouselco2`，内部的图片和文本的标识分别为`kdtest_imageap2`及`kdtest_labelap12`,字体图标标识为`kdtest_vectorap2`  
![2.png](https://vip.kingdee.com/download/0100653beb7666f94756bdc286475023ef90.png)  
2.通过setData添加数据


    
    
    */***
    * * 向轮播容器中动态添加三条数据*
    * * 包含 图片、标签控件。*
    * */*
    MessageCarouselContainer mcc = this.getControl("kdtest_messagecarouselco2"); //示例中我们的标识是`lbtap`
    List<Map<String, Object>> data = new ArrayList<>();
    *// 图片控件url数组*
    // 假设我们在该路径下有对应的数据
    String[] urls = new String[] {"/private/subscribe_pic1.png", "/private/subscribe_pic2.png", "/private/subscribe_pic3.png"};
    *// 标签控件显示内容数组*
    String[] texts = new String[] {"客户至上，用心服务，金蝶云总经理李帆给您的亲笔信", "致敬每一位勇敢拥抱数字化浪潮的人", "企业数
    字化转型高峰论坛"};
    *// 如果含有字体图标*
    String[] iconfonts = new String[] {"kdfont kdfont-tianjia", "kdfont kdfont-wendang", "kdfont kdfont-shangchuan"};
    *// 放内容map*
    Map<String, Object> propsMap = null;
    *// 控件map，key为控件类型*
    Map<String, Object> controlsMap = null;
    for (int i = 0; i < urls.length; i++;) {foopr
      propsMap = new HashMap<>();
      controlsMap = new HashMap<>();
      
      propsMap.put("text", texts[i]);
      controlsMap.put("kdtest_labelap12", propsMap); // kdtest_labelap12是示例中的文本控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("imageKey", urls[i]);  
      controlsMap.put("kdtest_imageap2", propsMap); // kdtest_imageap2是图片控件标识
      
      propsMap = new HashMap<>();
      propsMap.put("fontClass", iconfonts[i]);
      controlsMap.put("kdtest_vectorap2", propsMap); // kdtest_vectorap2是字体图标标识
     
      data.add(controlsMap);
    }
    
    mcc.setData(data);
    
    

  3. 返回数据示例


  * 在对应的请求中可以找到我们刚刚设置的数据  
![3.png](https://vip.kingdee.com/download/01008c371e899be24ddb92724b3b449399a0.png)


  4. 页面展示如图  
![4.png](https://vip.kingdee.com/download/01001e0ba84ae2064b9fbec42423cce07844.png)

  5. 如需进行事件监听，可以采用以下监听方法



  * 事件监听


    
    
    *// 实现「ListboxClickListener」接口*public class TestPlugin extends AbstractFormPlugin implements ItemClickListener, ClickListner {
      public void registerListener(EventObject event) {
    
        *// 注册监听*
        MessageCarouselContainer mcc = this.getView().getControl("控件标识");
        mcc.addItemClickListener(this);
        mcc.addClickListener(this);
      }
    
      *// 一行中的子项点击事件 发生之前，可以取消事件继续*
      public void beforeItemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 一行中的子项点击事件*
      public void itemClick(BeforeItemClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件 发生之前，可以取消事件继续*
      public void beforeClick(BeforeClickEvnet event) {
        *// do something...*
      }
      
      *// 行点击事件*
      public void click(BeforeClickEvnet event) {
        *// do something...*
      }
    }
    
    

__上一篇：分割容器

下一篇：过滤面板控件

 __

4.0 4人评分

内容反馈

*  __评论
收藏 7 

![复制链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_link_pc.png)复制链接

![复制标题链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_l.png)复制标题+链接

分享

 __

扫一扫用手机继续查看

![vip.kingdee.com]()

 __手机播放

编辑举报
