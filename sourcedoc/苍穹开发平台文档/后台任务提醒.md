# 后台任务提醒

原文链接：https://vip.kingdee.com/knowledge/specialDetail/218022218066869248?category=218035603349614848&id=669939940880079104&type=Knowledge&productLineId=29&lang=zh-CN

## 后台任务提醒

 __

[![金蝶云社区-hana](https://vip.kingdee.com/download/010162cfa9d5437711e8b441060400ef5315.png)](javascript:;)

hana

更新于 2025-07-10 15:53

浏览数： 1,245 

一级标题

二级标题

三级标题

四级标题

五级标题

六级标题

添加图片链接

上传图片 
    
    
      
    # 1 功能介绍
     它是一种后台事件处理的状态反馈容器，通俗点讲它就是用来展示进入后台运行任务的状态
    
    # 2 使用场景
        主要是针对系统进行的一些异步处理，如批量审核，批量删除，批量生成凭证、引入引出的状态等耗时且需要跳出当前转后台的一些动作
        
    # 3 展示形式
    无后台任务列表时，不会有任何展示;
    有后台运行列表时，它在页面内有两种展示形式:
         - 嵌入式：以控件形式嵌入到页面任意部位
    ![1.png](/download/010094162026f95743b987ff243fda0128b7.png)
         - 全局式：以全局控件悬浮球方式初始化放置在页面右下角,可以拖拽页面任意位置
    ![2.png](/download/0100c104358e5a2944ebb34b8bc718d48ec9.png)
    
    # 4 如何使用
    ## 4.1确认展示形式
        以嵌入式方式还是全局控件方式展示
    ## 4.2进行控件位置布局
        假如确认以全局控件方式展示，这个步骤可以跳过
        确认这个控件放置的位置，并在设计器中进行布局
    ![3.png](/download/010011a0ba5455254726a6f7c535e7f6ab7e.png)
    ## 4.3业务开发写对应的业务插件
    ## 4.4 把相应任务转为后台运行
    ![4.png](/download/0100e3bd1ceddb24459f86c883b8d9ead8be.png)
    ![5.png](/download/01009e1e02ca25dd404186bfa1ab173b48bf.png)
     注意：把相应任务转为后台运行，你就可以进行其他任意操作
    ## 4.5 展示大任务后台处理控件
           -- 嵌入式
    ![6.png](/download/0100fa20d61d4dbe4f48a4f5f96ac6a7315c.png)
       注意: 控件后面展示的是全部的任务数量
          -- 全局式
    ![7.png](/download/01009423fab3e3684a0f8918fdf781811495.png)
     注意: 悬浮球中间展示的是完成任务占所有任务的百分比，悬停时候会展示运行中的任务个数
    ## 4.6 查看任务列表详情
         点击控件，展开任务列表
    ![8.png](/download/0100fab99a62b3c348f1af00087315aa7058.png)
         
    ## 4.7 查看具体任务条目详情
    ![9.png](/download/01008b59998bb670458689d2081450975218.png)
         
    注意: 已完成的任务在查看详情之后会从任务列表里面移除
    ## 4.8 清除已完成任务
    ![10.png](/download/0100b1c6d0e08e3b4c8eb9a18760ae7dba99.png)
    
    注意: 点击清除按钮会把已完成的任务清除，包含: 已完成、已取消、任务异常
    ## 4.9 收起任务列表
    嵌入式:  点击列表外区域或再次点击嵌入球
    悬浮球:  点击非列表区域关闭任务列表
    
    # 5 插件示例
    
    ```
     private void dispatch() { 
      JobInfo jobInfo = new JobInfo(); // 创建任务目标 
      jobInfo.setAppId("bos"); // 执行类所在的应用名 
      jobInfo.setJobType(JobType.REALTIME); // 即时执行 
      jobInfo.setRunByUserId(Long.valueOf(RequestContext.get().getUserId())); jobInfo.setName("test job"); 
      jobInfo.setId(UUID.randomUUID().toString()); // 随机产生一个JobId 
      jobInfo.setTaskClassname("kd.bos.schedule.sample.MyTask"); 
      Map<String,Object> params = new HashMap<>(); params.put("time", 40); // 自定义参数，示例任务执行循环次数，80次 
      jobInfo.setParams(params); // 回调参数，设置一个回调处理标识(actionId) 
      CloseCallBack closeCallBack = new CloseCallBack(this, "taskcloseback"); // 创建任务进度界面参数 JobFormInfo 
      jobFormInfo = new JobFormInfo(jobInfo); jobFormInfo.setCaption("任务执行进度"); // 进度界面标题 
      jobFormInfo.setCloseCallBack(closeCallBack); // 任务完成后的回调处理 
      jobFormInfo.setCanBackground(true); // 允许切换到后台执行 
      jobFormInfo.setCanStop(true); // 允许中途取消 
      jobFormInfo.setClickClassName("kd.bos.schedule.sample.MyTaskClick"); // 发布任务，并显示进度 
      JobForm.dispatch(jobFormInfo, this.getView()); }
    
    
    ```
    
    ### 
    ```
         /** * 用户点击了前端任务提醒面板上的任务 */ 
         @Override public void click(ClickEventArgs e) { 
         // 获取任务执行结果 
         TaskInfo taskInfo = this.queryTask(); 
         // 获取首页（不会为null） //
         IFormView mainView = this.getMainView(); 
         // 获取发起任务的页面（如果用户已经关闭了发起任务的页面，返回null） 
         IFormView parentView = this.getParentView(); 
         if (taskInfo.isTaskEnd()) { 
           // 任务已经结束 
           e.setClearTask(true); // 通知平台，清除前端任务提醒图标 
           this.getMainView().showMessage("任务已经结束，可以在此处打开结果查看界面"); 
         } else { 
           // 任务没有结束，重新打开进度界面 
           if (parentView == null) { 
             this.getMainView().showMessage("启动任务的页面已经关闭，不能再打开进度界面"); 
           } else { this.showProgressForm(parentView); } } }
    
    
    ```
    
    ### 
    ```
    /** * 用户点击了前端任务面板上的清除按钮，清除已完成的任务条目 * * @return 默认返回true，同意前端清除此任务；如果认为时机不合适，还不希望任务条目被清除，可以返回false */ 
      @Override public boolean release() { 
        // TODO 释放资源、缓存等 
        return true; 
      }
    
    ```
     

# 1 功能介绍

它是一种后台事件处理的状态反馈容器，通俗点讲它就是用来展示进入后台运行任务的状态

# 2 使用场景
    
    
    主要是针对系统进行的一些异步处理，如批量审核，批量删除，批量生成凭证、引入引出的状态等耗时且需要跳出当前转后台的一些动作
    

# 3 展示形式

无后台任务列表时，不会有任何展示;  
有后台运行列表时，它在页面内有两种展示形式:  
\- 嵌入式：以控件形式嵌入到页面任意部位  
![1.png](https://vip.kingdee.com/download/010094162026f95743b987ff243fda0128b7.png)  
\- 全局式：以全局控件悬浮球方式初始化放置在页面右下角,可以拖拽页面任意位置  
![2.png](https://vip.kingdee.com/download/0100c104358e5a2944ebb34b8bc718d48ec9.png)

# 4 如何使用

## 4.1确认展示形式
    
    
    以嵌入式方式还是全局控件方式展示
    

## 4.2进行控件位置布局
    
    
    假如确认以全局控件方式展示，这个步骤可以跳过
    确认这个控件放置的位置，并在设计器中进行布局
    

![3.png](https://vip.kingdee.com/download/010011a0ba5455254726a6f7c535e7f6ab7e.png)

## 4.3业务开发写对应的业务插件

## 4.4 把相应任务转为后台运行

![4.png](https://vip.kingdee.com/download/0100e3bd1ceddb24459f86c883b8d9ead8be.png)  
![5.png](https://vip.kingdee.com/download/01009e1e02ca25dd404186bfa1ab173b48bf.png)  
注意：把相应任务转为后台运行，你就可以进行其他任意操作

## 4.5 展示大任务后台处理控件
    
    
       -- 嵌入式
    

![6.png](https://vip.kingdee.com/download/0100fa20d61d4dbe4f48a4f5f96ac6a7315c.png)  
注意: 控件后面展示的是全部的任务数量  
– 全局式  
![7.png](https://vip.kingdee.com/download/01009423fab3e3684a0f8918fdf781811495.png)  
注意: 悬浮球中间展示的是完成任务占所有任务的百分比，悬停时候会展示运行中的任务个数

## 4.6 查看任务列表详情
    
    
     点击控件，展开任务列表
    

![8.png](https://vip.kingdee.com/download/0100fab99a62b3c348f1af00087315aa7058.png)

## 4.7 查看具体任务条目详情

![9.png](https://vip.kingdee.com/download/01008b59998bb670458689d2081450975218.png)

注意: 已完成的任务在查看详情之后会从任务列表里面移除

## 4.8 清除已完成任务

![10.png](https://vip.kingdee.com/download/0100b1c6d0e08e3b4c8eb9a18760ae7dba99.png)

注意: 点击清除按钮会把已完成的任务清除，包含: 已完成、已取消、任务异常

## 4.9 收起任务列表

嵌入式: 点击列表外区域或再次点击嵌入球  
悬浮球: 点击非列表区域关闭任务列表

# 5 插件示例
    
    
     private void dispatch() { 
      JobInfo jobInfo = new JobInfo(); // 创建任务目标 
      jobInfo.setAppId("bos"); // 执行类所在的应用名 
      jobInfo.setJobType(JobType.REALTIME); // 即时执行 
      jobInfo.setRunByUserId(Long.valueOf(RequestContext.get().getUserId())); jobInfo.setName("test job"); 
      jobInfo.setId(UUID.randomUUID().toString()); // 随机产生一个JobId 
      jobInfo.setTaskClassname("kd.bos.schedule.sample.MyTask"); 
      Map<String,Object> params = new HashMap<>(); params.put("time", 40); // 自定义参数，示例任务执行循环次数，80次 
      jobInfo.setParams(params); // 回调参数，设置一个回调处理标识(actionId) 
      CloseCallBack closeCallBack = new CloseCallBack(this, "taskcloseback"); // 创建任务进度界面参数 JobFormInfo 
      jobFormInfo = new JobFormInfo(jobInfo); jobFormInfo.setCaption("任务执行进度"); // 进度界面标题 
      jobFormInfo.setCloseCallBack(closeCallBack); // 任务完成后的回调处理 
      jobFormInfo.setCanBackground(true); // 允许切换到后台执行 
      jobFormInfo.setCanStop(true); // 允许中途取消 
      jobFormInfo.setClickClassName("kd.bos.schedule.sample.MyTaskClick"); // 发布任务，并显示进度 
      JobForm.dispatch(jobFormInfo, this.getView()); }
    
    
    

### 
    
    
         /** * 用户点击了前端任务提醒面板上的任务 */ 
         @Override public void click(ClickEventArgs e) { 
         // 获取任务执行结果 
         TaskInfo taskInfo = this.queryTask(); 
         // 获取首页（不会为null） //
         IFormView mainView = this.getMainView(); 
         // 获取发起任务的页面（如果用户已经关闭了发起任务的页面，返回null） 
         IFormView parentView = this.getParentView(); 
         if (taskInfo.isTaskEnd()) { 
           // 任务已经结束 
           e.setClearTask(true); // 通知平台，清除前端任务提醒图标 
           this.getMainView().showMessage("任务已经结束，可以在此处打开结果查看界面"); 
         } else { 
           // 任务没有结束，重新打开进度界面 
           if (parentView == null) { 
             this.getMainView().showMessage("启动任务的页面已经关闭，不能再打开进度界面"); 
           } else { this.showProgressForm(parentView); } } }
    
    
    

### 
    
    
    /** * 用户点击了前端任务面板上的清除按钮，清除已完成的任务条目 * * @return 默认返回true，同意前端清除此任务；如果认为时机不合适，还不希望任务条目被清除，可以返回false */ 
      @Override public boolean release() { 
        // TODO 释放资源、缓存等 
        return true; 
      }
    
    

<h1><a id="1__0"></a>1 功能介绍</h1> <p>它是一种后台事件处理的状态反馈容器，通俗点讲它就是用来展示进入后台运行任务的状态</p> <h1><a id="2__3"></a>2 使用场景</h1> <pre><code>主要是针对系统进行的一些异步处理，如批量审核，批量删除，批量生成凭证、引入引出的状态等耗时且需要跳出当前转后台的一些动作 </code></pre> <h1><a id="3__6"></a>3 展示形式</h1> <p>无后台任务列表时，不会有任何展示;<br /> 有后台运行列表时，它在页面内有两种展示形式:<br /> \- 嵌入式：以控件形式嵌入到页面任意部位<br /> <img src="/download/010094162026f95743b987ff243fda0128b7.png" alt="1.png" /><br /> \- 全局式：以全局控件悬浮球方式初始化放置在页面右下角,可以拖拽页面任意位置<br /> <img src="/download/0100c104358e5a2944ebb34b8bc718d48ec9.png" alt="2.png" /></p> <h1><a id="4__14"></a>4 如何使用</h1> <h2><a id="41_15"></a>4.1确认展示形式</h2> <pre><code>以嵌入式方式还是全局控件方式展示 </code></pre> <h2><a id="42_17"></a>4.2进行控件位置布局</h2> <pre><code>假如确认以全局控件方式展示，这个步骤可以跳过 确认这个控件放置的位置，并在设计器中进行布局 </code></pre> <p><img src="/download/010011a0ba5455254726a6f7c535e7f6ab7e.png" alt="3.png" /></p> <h2><a id="43_21"></a>4.3业务开发写对应的业务插件</h2> <h2><a id="44__22"></a>4.4 把相应任务转为后台运行</h2> <p><img src="/download/0100e3bd1ceddb24459f86c883b8d9ead8be.png" alt="4.png" /><br /> <img src="/download/01009e1e02ca25dd404186bfa1ab173b48bf.png" alt="5.png" /><br /> 注意：把相应任务转为后台运行，你就可以进行其他任意操作</p> <h2><a id="45__26"></a>4.5 展示大任务后台处理控件</h2> <pre><code> \-- 嵌入式 </code></pre> <p><img src="/download/0100fa20d61d4dbe4f48a4f5f96ac6a7315c.png" alt="6.png" /><br /> 注意: 控件后面展示的是全部的任务数量<br /> – 全局式<br /> <img src="/download/01009423fab3e3684a0f8918fdf781811495.png" alt="7.png" /><br /> 注意: 悬浮球中间展示的是完成任务占所有任务的百分比，悬停时候会展示运行中的任务个数</p> <h2><a id="46__33"></a>4.6 查看任务列表详情</h2> <pre><code> 点击控件，展开任务列表 </code></pre> <p><img src="/download/0100fab99a62b3c348f1af00087315aa7058.png" alt="8.png" /></p> <h2><a id="47__37"></a>4.7 查看具体任务条目详情</h2> <p><img src="/download/01008b59998bb670458689d2081450975218.png" alt="9.png" /></p> <p>注意: 已完成的任务在查看详情之后会从任务列表里面移除</p> <h2><a id="48__41"></a>4.8 清除已完成任务</h2> <p><img src="/download/0100b1c6d0e08e3b4c8eb9a18760ae7dba99.png" alt="10.png" /></p> <p>注意: 点击清除按钮会把已完成的任务清除，包含: 已完成、已取消、任务异常</p> <h2><a id="49__45"></a>4.9 收起任务列表</h2> <p>嵌入式: 点击列表外区域或再次点击嵌入球<br /> 悬浮球: 点击非列表区域关闭任务列表</p> <h1><a id="5__49"></a>5 插件示例</h1> <pre><code class="lang-"> private void dispatch() { JobInfo jobInfo = new JobInfo(); // 创建任务目标 jobInfo.setAppId(&quot;bos&quot;); // 执行类所在的应用名 jobInfo.setJobType(JobType.REALTIME); // 即时执行 jobInfo.setRunByUserId(Long.valueOf(RequestContext.get().getUserId())); jobInfo.setName(&quot;test job&quot;); jobInfo.setId(UUID.randomUUID().toString()); // 随机产生一个JobId jobInfo.setTaskClassname(&quot;kd.bos.schedule.sample.MyTask&quot;); Map&lt;String,Object&gt; params = new HashMap&lt;&gt;(); params.put(&quot;time&quot;, 40); // 自定义参数，示例任务执行循环次数，80次 jobInfo.setParams(params); // 回调参数，设置一个回调处理标识(actionId) CloseCallBack closeCallBack = new CloseCallBack(this, &quot;taskcloseback&quot;); // 创建任务进度界面参数 JobFormInfo jobFormInfo = new JobFormInfo(jobInfo); jobFormInfo.setCaption(&quot;任务执行进度&quot;); // 进度界面标题 jobFormInfo.setCloseCallBack(closeCallBack); // 任务完成后的回调处理 jobFormInfo.setCanBackground(true); // 允许切换到后台执行 jobFormInfo.setCanStop(true); // 允许中途取消 jobFormInfo.setClickClassName(&quot;kd.bos.schedule.sample.MyTaskClick&quot;); // 发布任务，并显示进度 JobForm.dispatch(jobFormInfo, this.getView()); } </code></pre> <h3><a id="_72"></a></h3> <pre><code class="lang-"> /** * 用户点击了前端任务提醒面板上的任务 */ @Override public void click(ClickEventArgs e) { // 获取任务执行结果 TaskInfo taskInfo = this.queryTask(); // 获取首页（不会为null） // IFormView mainView = this.getMainView(); // 获取发起任务的页面（如果用户已经关闭了发起任务的页面，返回null） IFormView parentView = this.getParentView(); if (taskInfo.isTaskEnd()) { // 任务已经结束 e.setClearTask(true); // 通知平台，清除前端任务提醒图标 this.getMainView().showMessage(&quot;任务已经结束，可以在此处打开结果查看界面&quot;); } else { // 任务没有结束，重新打开进度界面 if (parentView == null) { this.getMainView().showMessage(&quot;启动任务的页面已经关闭，不能再打开进度界面&quot;); } else { this.showProgressForm(parentView); } } } </code></pre> <h3><a id="_95"></a></h3> <pre><code class="lang-">/** * 用户点击了前端任务面板上的清除按钮，清除已完成的任务条目 * * @return 默认返回true，同意前端清除此任务；如果认为时机不合适，还不希望任务条目被清除，可以返回false */ @Override public boolean release() { // TODO 释放资源、缓存等 return true; } </code></pre>

导航目录 __

# 1 功能介绍

它是一种后台事件处理的状态反馈容器，通俗点讲它就是用来展示进入后台运行任务的状态

# 2 使用场景
    
    
    主要是针对系统进行的一些异步处理，如批量审核，批量删除，批量生成凭证、引入引出的状态等耗时且需要跳出当前转后台的一些动作
    

# 3 展示形式

无后台任务列表时，不会有任何展示;  
有后台运行列表时，它在页面内有两种展示形式:  
\- 嵌入式：以控件形式嵌入到页面任意部位  
![1.png](https://vip.kingdee.com/download/010094162026f95743b987ff243fda0128b7.png)  
\- 全局式：以全局控件悬浮球方式初始化放置在页面右下角,可以拖拽页面任意位置  
![2.png](https://vip.kingdee.com/download/0100c104358e5a2944ebb34b8bc718d48ec9.png)

# 4 如何使用

## 4.1确认展示形式
    
    
    以嵌入式方式还是全局控件方式展示
    

## 4.2进行控件位置布局
    
    
    假如确认以全局控件方式展示，这个步骤可以跳过
    确认这个控件放置的位置，并在设计器中进行布局
    

![3.png](https://vip.kingdee.com/download/010011a0ba5455254726a6f7c535e7f6ab7e.png)

## 4.3业务开发写对应的业务插件

## 4.4 把相应任务转为后台运行

![4.png](https://vip.kingdee.com/download/0100e3bd1ceddb24459f86c883b8d9ead8be.png)  
![5.png](https://vip.kingdee.com/download/01009e1e02ca25dd404186bfa1ab173b48bf.png)  
注意：把相应任务转为后台运行，你就可以进行其他任意操作

## 4.5 展示大任务后台处理控件
    
    
       -- 嵌入式
    

![6.png](https://vip.kingdee.com/download/0100fa20d61d4dbe4f48a4f5f96ac6a7315c.png)  
注意: 控件后面展示的是全部的任务数量  
– 全局式  
![7.png](https://vip.kingdee.com/download/01009423fab3e3684a0f8918fdf781811495.png)  
注意: 悬浮球中间展示的是完成任务占所有任务的百分比，悬停时候会展示运行中的任务个数

## 4.6 查看任务列表详情
    
    
     点击控件，展开任务列表
    

![8.png](https://vip.kingdee.com/download/0100fab99a62b3c348f1af00087315aa7058.png)

## 4.7 查看具体任务条目详情

![9.png](https://vip.kingdee.com/download/01008b59998bb670458689d2081450975218.png)

注意: 已完成的任务在查看详情之后会从任务列表里面移除

## 4.8 清除已完成任务

![10.png](https://vip.kingdee.com/download/0100b1c6d0e08e3b4c8eb9a18760ae7dba99.png)

注意: 点击清除按钮会把已完成的任务清除，包含: 已完成、已取消、任务异常

## 4.9 收起任务列表

嵌入式: 点击列表外区域或再次点击嵌入球  
悬浮球: 点击非列表区域关闭任务列表

# 5 插件示例
    
    
     private void dispatch() { 
      JobInfo jobInfo = new JobInfo(); // 创建任务目标 
      jobInfo.setAppId("bos"); // 执行类所在的应用名 
      jobInfo.setJobType(JobType.REALTIME); // 即时执行 
      jobInfo.setRunByUserId(Long.valueOf(RequestContext.get().getUserId())); jobInfo.setName("test job"); 
      jobInfo.setId(UUID.randomUUID().toString()); // 随机产生一个JobId 
      jobInfo.setTaskClassname("kd.bos.schedule.sample.MyTask"); 
      Map<String,Object> params = new HashMap<>(); params.put("time", 40); // 自定义参数，示例任务执行循环次数，80次 
      jobInfo.setParams(params); // 回调参数，设置一个回调处理标识(actionId) 
      CloseCallBack closeCallBack = new CloseCallBack(this, "taskcloseback"); // 创建任务进度界面参数 JobFormInfo 
      jobFormInfo = new JobFormInfo(jobInfo); jobFormInfo.setCaption("任务执行进度"); // 进度界面标题 
      jobFormInfo.setCloseCallBack(closeCallBack); // 任务完成后的回调处理 
      jobFormInfo.setCanBackground(true); // 允许切换到后台执行 
      jobFormInfo.setCanStop(true); // 允许中途取消 
      jobFormInfo.setClickClassName("kd.bos.schedule.sample.MyTaskClick"); // 发布任务，并显示进度 
      JobForm.dispatch(jobFormInfo, this.getView()); }
    
    
    

### 
    
    
         /** * 用户点击了前端任务提醒面板上的任务 */ 
         @Override public void click(ClickEventArgs e) { 
         // 获取任务执行结果 
         TaskInfo taskInfo = this.queryTask(); 
         // 获取首页（不会为null） //
         IFormView mainView = this.getMainView(); 
         // 获取发起任务的页面（如果用户已经关闭了发起任务的页面，返回null） 
         IFormView parentView = this.getParentView(); 
         if (taskInfo.isTaskEnd()) { 
           // 任务已经结束 
           e.setClearTask(true); // 通知平台，清除前端任务提醒图标 
           this.getMainView().showMessage("任务已经结束，可以在此处打开结果查看界面"); 
         } else { 
           // 任务没有结束，重新打开进度界面 
           if (parentView == null) { 
             this.getMainView().showMessage("启动任务的页面已经关闭，不能再打开进度界面"); 
           } else { this.showProgressForm(parentView); } } }
    
    
    

### 
    
    
    /** * 用户点击了前端任务面板上的清除按钮，清除已完成的任务条目 * * @return 默认返回true，同意前端清除此任务；如果认为时机不合适，还不希望任务条目被清除，可以返回false */ 
      @Override public boolean release() { 
        // TODO 释放资源、缓存等 
        return true; 
      }
    
    

__上一篇：应用导航菜单

下一篇：通用过滤

 __

暂无评分

内容反馈

*  __评论
收藏 3 

![复制链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_link_pc.png)复制链接

![复制标题链接](https://cdn-vip.kingdee.com/statics/webfront/icon/copy_l.png)复制标题+链接

分享

 __

扫一扫用手机继续查看

![vip.kingdee.com]()

 __手机播放

编辑举报
